{% extends 'base.html.twig' %}

{% block title %}{{ customer.name }} {{ parent() }}{% endblock %}

{% block body %}
    <h1>{{ customer.name }}</h1>
    <p>Date de création : {{ customer.createdAt|date('d/m/Y') }}</p>
    <ul>
        {% for project in customer.projects %}
            <li>
                {{ project.title }}
                (
                {% for category in project.categories %}
                    {{ category.name }}
                {% endfor %}
                )
            </li>
        {% endfor %}
    </ul>
{% endblock %}

