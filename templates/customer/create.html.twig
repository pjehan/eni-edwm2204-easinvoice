{% extends 'base.html.twig' %}

{% block title %}Ajouter un client {{ parent() }}{% endblock %}

{% block body %}
    <h1>Ajouter un client</h1>

    {{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit" class="btn btn-success">
        Créer
    </button>

    {{ form_end(form) }}
{% endblock %}

