<ul class="dropdown-menu">
    {% for customer in customers %}
        <li>
            <a class="dropdown-item" href="{{ path('customer_show', {'id': customer.id}) }}">
                {{ customer.name }}
            </a>
        </li>
    {% endfor %}
</ul>

